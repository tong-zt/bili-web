# 把下面这一段放进宝塔：
# 网站 -> bld.atm6.cn -> 设置 -> 配置文件
# 放在 server { ... } 内部，然后保存并重载 Nginx。

location /downloads/ {
    alias /www/wwwroot/bili-web/downloads/;
    add_header Content-Disposition "attachment";
    sendfile on;
    tcp_nopush on;
}
