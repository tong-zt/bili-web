services:
  backend:
    build: ./backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./frontend:/app/frontend:ro
      - ./downloads:/data/downloads
